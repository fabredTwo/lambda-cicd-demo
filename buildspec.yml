version: 0.2

phases:
  build:
    commands:
      - echo "Empaquetando Lambda root..."
      - rm -f root.zip
      - cd function
      - zip -r ../root.zip .
      - cd ..

      - unzip -l root.zip
      - aws lambda update-function-code --function-name suma-cicd --zip-file fileb://root.zip
