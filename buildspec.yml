build:
    commands:
      - echo "Zippeando..."
      - zip -r function.zip app.py suma.py
      - echo "Subiendo a MI bucket manual..."
      # USA TU BUCKET MANUAL AQUÍ
      - aws s3 cp function.zip s3://cicd-bucket-uno/function.zip